# 디자인 자동 운영 (계속 업데이트)

4시간마다 실앱을 돌아다니며 디자인을 검사한다. **깨짐·가독성·에러는 바로 고치고, 완성도 개선은 제안→알림→승인** 순서로.

## 1. 두 트랙

| 트랙 | 대상 | 승인 | 실행 |
|---|---|---|---|
| **A · 자동수정** | 깨짐(오버플로·겹침·잘림), 에러(크래시·pageerror), 본문 명도 <4.5, 탭타겟 <24px, 폰트 <10, 줄간격 < 폰트, 제품규칙 위반(`code-check`) | 불필요 — 단, **몰래 커밋 없음**. 작업 세션(에이전트 또는 너)에서 고치고 CI 초록까지 확인 후 커밋. 모든 변경은 커밋·아티팩트에 증거 남음 | 에이전트 세션 (4h 이슈에 증거 첨부되면 다음 세션에서 즉시 반영) |
| **B · 제안** | 팔레트·간격·타이포 스케일 변경, 새 컴포넌트, 카피 톤, 벤치마크 기반 리디자인, **애니메이션·모션 추가/변경 전부** | 필수 — 이슈에 `/승인` 댓글 또는 `approved` 라벨 | 승인 후 에이전트 세션에서 적용 → 스크린샷+영상으로 증거 |

원칙: **네 승인 없이 바뀌는 건 고장 수리뿐이다.** 디자인(특히 모션)은 무조건 제안→승인. 자동화는 감시·증거·이슈까지만 하고, 코드는 세션에서 확인하고 넣는다.

## 2. 검사 수단 (12시간마다 + 푸시)

- `scripts/design-check.mjs` — 정적: D1 본문명도(FAIL) · D2 보조명도(WARN) · D3 폰트 · D4 줄간격 · D5 하드코딩색
- `scripts/visual-check.mjs` — 실앱 주행: 6개 라우트 스크린샷 vs 베이스라인(픽셀 회귀 0.5%), 가로 넘침, 탭타겟, pageerror + **라우트별 전환 시간(readyMs, 8초↑ 경고·15초 FAIL)**
- `scripts/livelike-check.mjs --smoke/full` — 사용성 주행 (클릭·게이트·상점규칙) + **전 과정 영상 저장(`reports/videos/`)**
- `.maestro/*.yaml` — 실기기 탭 여정 (UI 변경 푸시마다 + 월간)

## 모션 검증 정책 (솔직 버전)

| 항목 | 자동? | 방법 |
|---|---|---|
| 전환 결과 (원하는 화면에 도착) | ✅ | smoke·Maestro·walk가 텍스트로 확인 |
| 전환 시간 (느려터졌는지) | ✅ | visual readyMs 측정 (8초 경고·15초 실패) |
| 깨짐 없는 모션 (겹침·잘림·프리즈) | ✅ | 오버플로 탐지 + pageerror + 영상 |
| 모션 미학 (예쁜지·자연스러운지) | ❌ 사람 | 영상으로 네가 판단. 제안 이슈에 모션 스펙(시간·이징) 명시 필수 |
| 베이스라인 스크린샷의 애니메이션 OFF | 의도적 | 픽셀 비교 안정성용. 모션은 위 3줄+영상으로 본다 |

## 3. 벤치마크 (완성도 제안의 근거)

| 앱 | 참고 | 방향 |
|---|---|---|
| LiveLike | **스푼** | 라이브 오디오 UI — 방송 카드·리액션·기프트·댓글 흐름의 밀도와 리듬 |
| WorldWideSomeone | **젠리·인스타** | 친구 맵의 가벼움(젠리) + 피드·스토리·DM의 익숙함(인스타). 현 팔레트(v3)도 이 계열 |
| Beff | **젠리·제타** | 친구 맵(젠리) + 10대 채팅·관계 UI(제타)의 캐주얼함 |

제안 이슈에는 반드시 **현 스크린샷 vs 제안안 + 참고앱의 해당 화면 근거**를 첨부한다.

## 4. 승인·알림 플로우

1. 제안이 `design-proposal` 이슈로 열리면 Watch 중인 너에게 **GitHub 알림(이메일·모바일 푸시)** 이 간다.
2. 이슈 댓글에 `/승인` 이라고 쓰면 (`design-approve.yml`) `approved` 라벨이 붙는다.
3. `approved` 라벨 이슈는 다음 에이전트 세션에서 적용 → 스크린샷 증거와 함께 닫힘.
4. 반려면 `/반려 사유` 댓글 — 이슈는 닫히고 사유가 기록된다.

긴급(트랙 A) 이슈는 라벨 `auto-fix`로 열리고 승인 없이 다음 세션에서 바로 반영된다.

## 5. 현재 제안 큐 (design-check WARN + 2026-09-21 전수조사)

정적 수집:
- 라이트 tertiary(`text3`) 명도 2.38 — 캡션 가독성 (인스타 기준과 비교 제안)
- 라이트 파랑/빨강/초록 본문 사용 시 명도 부족 — 링크·대형 전용 규칙 명문화
- `ui/index.tsx` fontSize 10 — 캡션 토큰(11)으로 통일
- 하드코딩 색상 4개 파일 — 토큰 이전 (`(tabs)/index`, `store`, `postcard-thumb`, `signup-sheet`)

전수조사 시드 (`docs/REFS-WWS.md`, 제안 이슈로 분할 등록 예정):
- **#1 tertiary 격하**: 링·카드 본문에서 `text3` 퇴출 → 플레이스홀더·비활성 전용 (I1·I2 근거)
- **#2 피드 카드 인스타 규격화** + postcard-thumb 토큰 정리 (I2)
- **#3 국가 스토리 링 상태** (본/미본) (I1)
- **#4 잡기 테이크오버** 햅틱+풀스크린 (B3)
- **#5 홈 바텀시트 3단 확장** (B2)
- **#6 친구 발자취 지도** in 프로필 (B4)
- **#7 플랜 비교 3열표** in 상점 (I4)
- **#8 피드 스켈레톤 로딩** (I6)

## 6. 베이스라인 갱신

의도적 디자인 변경 후에는 베이스라인을 재생성해야 한다 (안 하면 다음 4h에서 회귀로 뜬다):

```bash
BASE=http://localhost:8081 node scripts/visual-check.mjs --update
# 또는 Actions 수동 실행 → update_baseline 체크
```
